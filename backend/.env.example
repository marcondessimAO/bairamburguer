# Ficheiro de referência: copie para .env e preencha com os valores reais
# NUNCA faça commit do ficheiro .env com valores reais!

# ─── Mercado Pago ───────────────────────────────────────────────────────────
MERCADOPAGO_ACCESS_TOKEN=APP_USR-xxxx-seu-token-real-aqui

# ─── JWT ────────────────────────────────────────────────────────────────────
JWT_SECRET=uma-chave-secreta-longa-e-aleatoria-para-producao

# ─── Base de Dados (Docker) ──────────────────────────────────────────────────
# (usadas internamente pelo docker-compose, já definidas lá)
# POSTGRES_USER=bairam_admin
# POSTGRES_PASSWORD=sua-senha-segura
# POSTGRES_DB=bairamburguer
