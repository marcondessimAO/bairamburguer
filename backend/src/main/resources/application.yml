spring:
  application:
    name: bairamburguer-api
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 88jr83w88
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
